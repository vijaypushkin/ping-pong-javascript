export const DIRECTION = {
    IDLE: 0,
    UP: 1,
    DOWN: 2
}

export const BALL_POS_X = '--position-x';
export const BALL_POS_Y = '--position-y';

// ? using this constant to control the speed of the ball
export const BALL_VELOCITY = 0.05;

export const PADDLE_POS = '--position';

// ? using this constant to control the speed of the paddle
export const PADDLE_VELOCITY = 0.05;